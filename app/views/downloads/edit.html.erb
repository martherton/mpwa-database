<%= simple_form_for [@user, @download], url: download_path, method: :put do |f| %>

	<%= f.input :title %>
	<%= f.input :description %>
	<%= f.input :wordoc %>
	
	
	<%= f.button :submit, "Add information" %>
	* Required field
<% end %>